Holds the tests for our classes

Tests Included:
  AppTest: Tests the Functionality of the app object
  Comment_AddTest: Tests that the comments are added to the gui
  DatabaseTest: Tests that the files could be read from and the list could be manipulated
  UserTest: Tests the functionality of the standard User object
  
